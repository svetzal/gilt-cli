Dependency update complete. Only one package had a newer compatible version available:

- **`idna` 3.14 → 3.15** — a minor patch bump (internationalized domain names library, transitive dependency)

All three quality gates passed cleanly:
- ✅ **Tests**: 1134 passed, 59 skipped
- ✅ **Lint**: no issues
- ✅ **Build**: sdist + wheel built successfully

The lockfile update has been committed and pushed to `main`.